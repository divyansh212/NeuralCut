# Core runtime (mock mode runs on this alone — no torch needed).
fastapi>=0.110
uvicorn[standard]>=0.29
celery>=5.3
redis[hiredis]>=5.0
supabase>=2.4
pyjwt>=2.8
pydantic-settings>=2.2
pillow>=10.0
python-multipart>=0.0.9
requests>=2.31
streamlit>=1.33

# Uncomment for PROVIDER_MODE=live with PyTorch image generation
# (very large; only on a GPU host):
# torch>=2.2
# diffusers>=0.27
# transformers>=4.40
# accelerate>=0.29
# openai>=1.20
