# Copy to frontend/.env.local — Next.js auto-loads it.
# These mirror keys from the root .env.example so the frontend can talk
# to both Supabase (directly, for auth) and the FastAPI backend.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
